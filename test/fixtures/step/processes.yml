# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#

process_superuser_batch_ready:
  batch: superuser_batch_processing

process_superuser_batch:
  batch: superuser_batch_processed

# preprocess_superuser_batch_done:
#   batch: superuser_batch_transferred
#   done: true

process_fishmonger_batch:
  batch: fishmonger_batch_processed
