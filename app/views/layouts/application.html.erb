<!DOCTYPE html>
<html>
  <head>
    <title>CspaceBatchImport</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p id="notice"><%= notice %></p>
    <p id="alert"><%= alert %></p>

    <% if user_signed_in? %>
      <%= button_to t('user.logout'), destroy_user_session_path, method: :delete  %>
      <%= button_to "#{t('user.account')}: #{current_user.email}", edit_user_path(current_user), method: :get %>
      <% if current_user != true_user %>
        <%= button_to t('user.return'), stop_impersonating_users_path, method: :post %>
      <% end %>

      <ul>
        <li><%= link_to t('navigation.home'), root_path %></li>
        <% if current_user.admin? %>
          <li><%= link_to t('navigation.users'), users_path %></li>
        <% end %>
      </ul>
    <% end %>

    <%= yield %>
  </body>
</html>
