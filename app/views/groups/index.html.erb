<h1 class="title"><%= t('group.title') %></h1>

<%= render 'form', group: @group %>

<% policy_scope(@groups).each do |group| %>
  <%= content_tag :div, class: 'level'do ; end %>
  <%= render 'form', group: group %>
<% end %>
